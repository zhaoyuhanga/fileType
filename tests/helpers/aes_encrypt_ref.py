"""测试用 AES-128-CBC 加密参考实现（仅用于构造加密样本）。

刻意与产品代码 `core/video/aes.py` 分开写：
- 产品代码只实现解密（用户只需要解密自己有权观看的流）；
- 这里实现加密，用来**生成真实的加密样本**，从而验证产品解密端到端正确
  （断言下载结果与原始明文逐字节一致）。

参考实现自身也带已知答案测试（见 test_aes128_encrypt_ref_known_answer）。
"""
from __future__ import annotations

_SBOX = (
    ,
)
_RCON = (0x01, 0x02, 0x04, 0x08, 0x10, 0x20, 0x40, 0x80, 0x1B, 0x36)


def _xtime(value: int) -> int:
    value <<= 1
    return (value ^ 0x1B) & 0xFF if value & 0x100 else value


def _mul(a: int, b: int) -> int:
    result = 0
    for _ in range(8):
        if b & 1:
            result ^= a
        a = _xtime(a)
        b >>= 1
    return result & 0xFF


def _round_keys(key: bytes) -> list[list[int]]:
    words = [list(key[i * 4:i * 4 + 4]) for i in range(4)]
    for index in range(4, 44):
        temp = list(words[index - 1])
        if index % 4 == 0:
            temp = [_SBOX[b] for b in temp[1:] + temp[:1]]
            temp[0] ^= _RCON[index // 4 - 1]
        words.append([words[index - 4][i] ^ temp[i] for i in range(4)])
    return [sum(words[i * 4:i * 4 + 4], []) for i in range(11)]


def _shift_rows(state: list[int]) -> list[int]:
    return [
        state[0], state[5], state[10], state[15],
        state[4], state[9], state[14], state[3],
        state[8], state[13], state[2], state[7],
        state[12], state[1], state[6], state[11],
    ]


def _mix_columns(state: list[int]) -> list[int]:
    out = [0] * 16
    for column in range(4):
        offset = column * 4
        a0, a1, a2, a3 = state[offset:offset + 4]
        out[offset + 0] = _mul(a0, 2) ^ _mul(a1, 3) ^ a2 ^ a3
        out[offset + 1] = a0 ^ _mul(a1, 2) ^ _mul(a2, 3) ^ a3
        out[offset + 2] = a0 ^ a1 ^ _mul(a2, 2) ^ _mul(a3, 3)
        out[offset + 3] = _mul(a0, 3) ^ a1 ^ a2 ^ _mul(a3, 2)
    return out


def _encrypt_block(block: bytes, keys: list[list[int]]) -> bytes:
    state = [block[i] ^ keys[0][i] for i in range(16)]
    for round_index in range(1, 10):
        state = [_SBOX[b] for b in state]
        state = _shift_rows(state)
        state = _mix_columns(state)
        state = [state[i] ^ keys[round_index][i] for i in range(16)]
    state = [_SBOX[b] for b in state]
    state = _shift_rows(state)
    return bytes(state[i] ^ keys[10][i] for i in range(16))


def aes128_cbc_encrypt_ref(data: bytes, key: bytes, iv: bytes) -> bytes:
    """AES-128-CBC 加密并做 PKCS#7 填充（模拟 HLS 分片的加密方式）。"""
    if len(key) != 16 or len(iv) != 16:
        raise ValueError("AES-128 需要 16 字节密钥与 IV")
    pad = 16 - (len(data) % 16)
    padded = data + bytes([pad]) * pad

    keys = _round_keys(key)
    out = bytearray()
    previous = iv
    for offset in range(0, len(padded), 16):
        block = padded[offset:offset + 16]
        xored = bytes(block[i] ^ previous[i] for i in range(16))
        encrypted = _encrypt_block(xored, keys)
        out.extend(encrypted)
        previous = encrypted
    return bytes(out)


def test_aes128_encrypt_ref_known_answer() -> None:
    """参考实现自身必须先通过官方向量，否则它构造的"密文"不可信。"""
    import binascii

    key = binascii.unhexlify("000102030405060708090a0b0c0d0e0f")
    # FIPS-197 C.1：单分组（无填充时等价于 CBC 且 IV=0）
    plain = binascii.unhexlify("00112233445566778899aabbccddeeff")
    cipher = aes128_cbc_encrypt_ref(plain, key, b"\x00" * 16)
    # 带 PKCS#7 填充，所以前 16 字节应与官方密文一致
    assert cipher[:16] == binascii.unhexlify("69c4e0d86a7b0430d8cdb78070b4c55a")
