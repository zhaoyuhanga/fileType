feat(modu): 内嵌 main 分支前端承载“墨读转换”与文档预览（路线 A 基础）

- 前端：main 分支 renderer 经 Vite 构建并注入 qtwebchannel/bridge_shim，
  作为静态资源 webfront 随包携带
- 桥：services/web_bridge（QWebChannel bridge）把 window.formatFlow 映射到
  Python：文件导入/对话框/输出目录/引擎状态/批量 startJobs(逐文件事件+取消)/
  readDoc/saveDoc/saveDocAs
- boards/convert_web：真实桌面打开“墨读转换”时用 QtWebEngine 加载该前端
  （offscreen/无资源自动回退原 Qt 界面，测试不受影响）
- spec：datas 打入 webfront、hiddenimports 补 QtWebChannel 等
- 实测（真实桌面 WebView）：formatFlow 注入成功；getEngineStatus/importPaths/readDoc 桥接返回正确
- pytest 全量绿